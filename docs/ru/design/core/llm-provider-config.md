+++
title = "Проект Системы Конфигурации Провайдеров TOML"
description = """Система Конфигурации Провайдеров TOML мигрирует всю конфигурацию LLM Provider из жёстко закодированных значений в конфигурационные файлы TOML, достигая разделения конфигурации и кода, улучшая у"""
lang = "ru"
category = "design"
subcategory = "core"
+++

# Проект Системы Конфигурации Провайдеров TOML

## Обзор

Система Конфигурации Провайдеров TOML мигрирует всю конфигурацию LLM Provider из жёстко закодированных значений в конфигурационные файлы TOML, достигая разделения конфигурации и кода, улучшая удобство поддержки и расширяемость.

## Основные Цели

| Цель | Описание |
| --- | --- |
| Удобство поддержки | Конфигурация отделена от кода, не требуется перекомпиляция для изменений |
| Расширяемость | Добавление нового Провайдера требует только добавления файла TOML |
| Читаемость | Конфигурационные файлы ясны и легки для понимания |
| Повторное использование | Конфигурация может быть разделена между различными средами |

## Проект Архитектуры

### Процесс Загрузки Конфигурации

```mermaid
flowchart TB
    subgraph Initialization Phase
        A[Запуск Приложения] --> B[Сканировать Директорию res/]
        B --> C[Загрузить Все Файлы .toml]
        C --> D[Разобрать Структуру TOML]
    end

    subgraph Validation Phase
        D --> E{Проверить Полноту Конфигурации}
        E -->|Пройдено| F[Сохранить в Кэш Конфигурации]
        E -->|Не Пройдено| G[Записать Ошибку]
        G --> H[Использовать Конфигурацию по Умолчанию]
    end

    subgraph Runtime
        F --> I[Запрос Провайдера]
        I --> J[Получить Конфигурацию из Кэша]
        J --> K[Вернуть ProviderConfig]
    end
```

### Иерархия Конфигурации

```mermaid
graph TB
    subgraph ProviderConfig
        A[Информация о Провайдере]
        B[Конфигурация API]
        C[Конфигурация Лимитов]
        D[Конфигурация Цен]
        E[Конфигурация Возможностей]
        F[Список Моделей]
    end

    A --> A1[id, name, type, protocol]
    B --> B1[base_url, endpoints, auth]
    C --> C1[ограничения параллелизма, ограничения скорости, таймаут]
    D --> D1[режим биллинга, информация о квоте]
    E --> E1[streaming, vision, function_calling]
    F --> F1[Список ModelConfig]

    subgraph ModelConfig
        F1 --> M1[id, name, context_window]
        F1 --> M2[флаги поддержки возможностей]
        F1 --> M3[информация о ценах]
        F1 --> M4[данные бенчмарков]
    end
```

## Приоритет Конфигурации

```mermaid
graph LR
    A[Пользовательская Конфигурация] -->|Наивысший Приоритет| D[Эффективная Конфигурация]
    B[Конфигурация Сообщества] -->|Средний Приоритет| D
    C[Официальная Конфигурация] -->|Базовый Приоритет| D

    style A fill:#90EE90
    style B fill:#FFD700
    style C fill:#87CEEB
```

### Правила Слияния Приоритетов

| Слой | Источник | Описание |
| --- | --- | --- |
| 1 | Официальная Конфигурация | Данные официальной документации провайдера, как базовые значения по умолчанию |
| 2 | Конфигурация Сообщества | Оптимизированная конфигурация, предоставленная сообществом, переопределяет официальные данные |
| 3 | Пользовательская Конфигурация | Конфигурация, определённая пользователем, наивысший приоритет |

## Модели Ценообразования

```mermaid
stateDiagram-v2
    [*] --> PayAsYouGo: Оплата за Использование
    [*] --> OneTime: Единоразовая Покупка
    [*] --> Periodic: Периодическая Квота
    [*] --> Free: Бесплатно

    PayAsYouGo --> Учёт Использования
    OneTime --> Проверка Баланса
    Periodic --> Проверка Периодической Квоты
    Free --> Безлимитно
```

### Сравнение Моделей Ценообразования

| Модель | Применимые Сценарии | Характеристики |
| --- | --- | --- |
| PayAsYouGo | OpenAI, Anthropic | Оплата за токен, списание в реальном времени |
| OneTime | Предоплаченные пакеты | Предварительная покупка квоты, использование до исчерпания |
| Periodic | GLM China и др. | Периодический сброс квоты |
| Free | Локальные модели Ollama | Без ограничений стоимости |

## Классификация Типов Провайдеров

```mermaid
graph TB
    subgraph Cloud Providers
        A[OpenAI Совместимый Протокол]
        B[Anthropic Протокол]
        C[Google Gemini Протокол]
    end

    subgraph Local Providers
        D[Ollama]
        E[LocalAI]
    end

    subgraph Custom Providers
        F[Определяемые Пользователем Конечные Точки]
    end

    A --> A1[OpenAI, DeepSeek, Qwen]
    B --> B1[Серия Claude]
    C --> C1[Серия Gemini]
```

## Механизм Горячей Перезагрузки

```mermaid
sequenceDiagram
    participant FS as Файловая Система
    participant Watcher as Наблюдатель Конфигурации
    participant Cache as Кэш Конфигурации
    participant App as Приложение

    FS->>Watcher: Событие Изменения Файла
    Watcher->>Watcher: Разобрать Изменённое Содержимое
    Watcher->>Cache: Обновить Кэш
    Cache->>App: Отправить Уведомление об Обновлении Конфигурации
    App->>App: Применить Новую Конфигурацию
```

## Стратегия Обработки Ошибок

```mermaid
flowchart TB
    A[Загрузка Конфигурации] --> B{Разбор Успешен?}
    B -->|Да| C[Проверить Конфигурацию]
    B -->|Нет| D[Записать Ошибку Разбора]

    C --> E{Проверка Пройдена?}
    E -->|Да| F[Сохранить в Кэш]
    E -->|Нет| G[Записать Ошибку Проверки]

    D --> H[Использовать Конфигурацию по Умолчанию]
    G --> H

    F --> I[Нормальное Использование]
    H --> I
```

## Проект Расширяемости

### Добавление Нового Провайдера

```mermaid
flowchart LR
    A[Создать Файл TOML] --> B[Определить Информацию о Провайдере]
    B --> C[Настроить Конечные Точки API]
    C --> D[Добавить Список Моделей]
    D --> E[Установить Информацию о Ценах]
    E --> F[Перезапустить Приложение]
    F --> G[Автоматическая Загрузка Конфигурации]
```

### Правила Проверки Конфигурации

| Поле | Правило Проверки | Обработка Ошибки |
| --- | --- | --- |
| provider.id | Непустое, уникальное | Отклонить загрузку, записать ошибку |
| api.base_url | Допустимый формат URL | Использовать значение по умолчанию |
| models[].id | Непустое | Пропустить эту модель |
| pricing.model | Проверка значения перечисления | По умолчанию PayAsYouGo |

## Соображения Безопасности

```mermaid
flowchart TB
    subgraph Sensitive Info Handling
        A[API Ключ] --> B[Зашифрованное Хранение]
        B --> C[Использование в Памяти]
        C --> D[Маскирование в Логах]
    end

    subgraph Access Control
        E[Чтение Конфигурации] --> F{Проверка Разрешений}
        F -->|Есть Разрешение| G[Вернуть Конфигурацию]
        F -->|Нет Разрешения| H[Отказать в Доступе]
    end
```

## Будущие Расширения

| Функция | Описание | Приоритет |
| --- | --- | --- |
| Горячая Перезагрузка Конфигурации | Загрузка внешних файлов конфигурации во время выполнения | Высокий |
| Проверка Конфигурации | Проверка полноты конфигурации при запуске | Высокий |
| Слияние Конфигураций | Пользовательская конфигурация переопределяет конфигурацию по умолчанию | Средний |
| Импорт/Экспорт Конфигурации | Поддержка импорта/экспорта файлов конфигурации | Средний |
| Обновление Агента | Авто-обновление конфигурации из официальной документации | Низкий |

# Проект Управления Метаданными Провайдеров

## Обзор

Система Управления Метаданными Провайдеров отвечает за динамическое получение информации о конфигурации из официальной документации LLM Provider, обеспечивая автоматизированные обновления и проверку конфигурационных данных.

## Основная Проблема

Текущая реализация содержит жёстко закодированную статистику использования и не имеет поддержки динамических данных Провайдеров. Необходимо создать автоматизированный механизм получения и управления метаданными.

## Проект Архитектуры

### Архитектура Потока Данных

```mermaid
flowchart TB
    subgraph Data Sources
        A[Официальная Документация]
        B[Конечные Точки API]
        C[Вклад Сообщества]
    end

    subgraph Collection Layer
        D[Агент Конфигурации]
        E[Веб-Скрапер]
        F[API Клиент]
    end

    subgraph Processing Layer
        G[Парсер Данных]
        H[Движок Проверки]
        I[Стратегия Слияния]
    end

    subgraph Storage Layer
        J[База Данных Конфигурации]
        K[Слой Кэширования]
    end

    A --> D
    B --> F
    C --> D
    D --> G
    E --> G
    F --> G
    G --> H
    H --> I
    I --> J
    J --> K
```

### Модель Приоритета Конфигурации

```mermaid
graph TB
    subgraph Priority Layers
        A[Пользовательская Конфигурация] -->|Наивысший| D[Эффективная Конфигурация]
        B[Конфигурация Сообщества] -->|Средний| D
        C[Официальная Конфигурация] -->|Базовый| D
    end

    subgraph Merge Rules
        D --> E[Переопределение на Уровне Полей]
        E --> F[Сохранить Значение с Высоким Приоритетом]
    end
```

## Структура Метаданных

### Иерархия Конфигурации Провайдера

```mermaid
classDiagram
    class ProviderConfig {
        +provider_id: String
        +display_name: String
        +available_models: List~ModelConfig~
        +default_model: String
        +pricing_model: PricingModel
        +usage_type: UsageType
        +api_endpoint: String
    }

    class ModelConfig {
        +model_id: String
        +model_name: String
        +context_window: u64
        +max_output_tokens: u64
        +supports_vision: bool
        +supports_function_calling: bool
    }

    class PricingModel {
        <<enumeration>>
        OneTime
        Periodic
        PayAsYouGo
    }

    class UsageType {
        <<enumeration>>
        Metered
        Quota
        Unlimited
    }

    ProviderConfig --> ModelConfig
    ProviderConfig --> PricingModel
    ProviderConfig --> UsageType
```

### Классификация Источников Конфигурации

| Тип Источника | Описание | Надёжность | Частота Обновления |
| --- | --- | --- | --- |
| Официальный | Официальная документация провайдера | Высокая | Автоматическая периодическая |
| Сообщество | Данные, предоставленные сообществом | Средняя | Ручное обновление |
| Пользовательское Переопределение | Настроенное пользователем | Наивысшая | В реальном времени |

## Система Сбора Агентов

### Процесс Сбора

```mermaid
sequenceDiagram
    participant Scheduler as Планировщик
    participant Agent as Агент Конфигурации
    participant Source as Источник Данных
    participant Parser as Парсер
    participant Validator as Валидатор
    participant DB as База Данных

    Scheduler->>Agent: Запустить задачу сбора
    Agent->>Source: Запросить официальную документацию
    Source-->>Agent: Вернуть HTML/JSON
    Agent->>Parser: Разобрать содержимое
    Parser-->>Agent: Структурированные данные
    Agent->>Validator: Проверить данные
    Validator-->>Agent: Результат проверки
    Agent->>DB: Сохранить конфигурацию
    DB-->>Agent: Сохранение успешно
    Agent-->>Scheduler: Задача завершена
```

### Обязанности Агента Провайдера

```mermaid
flowchart LR
    subgraph OpenAI Agent
        A1[Получить список моделей]
        A2[Разобрать информацию о ценах]
        A3[Извлечь ограничения скорости]
    end

    subgraph Anthropic Agent
        B1[Получить модели Claude]
        B2[Разобрать контекстное окно]
        B3[Извлечь информацию о возможностях]
    end

    subgraph GLM Agent
        C1[Получить модели GLM]
        C2[Разобрать информацию о квоте]
        C3[Извлечь период сброса]
    end
```

## Механизм Проверки Данных

### Процесс Проверки

```mermaid
flowchart TB
    A[Получить данные конфигурации] --> B{Проверка формата}
    B -->|Пройдено| C{Логическая проверка}
    B -->|Не Пройдено| D[Записать ошибку]

    C -->|Пройдено| E{Проверка полноты}
    C -->|Не Пройдено| D

    E -->|Пройдено| F{Проверка согласованности}
    E -->|Не Пройдено| G[Заполнить значения по умолчанию]

    F -->|Пройдено| H[Принять конфигурацию]
    F -->|Не Пройдено| I[Пометить для проверки]

    G --> F
    D --> J[Отклонить конфигурацию]
```

### Правила Проверки

| Тип Проверки | Содержание Проверки | Обработка Сбоя |
| --- | --- | --- |
| Проверка формата | Типы данных, форматы полей | Отклонить и записать |
| Логическая проверка | Диапазоны значений, значения перечислений | Использовать значения по умолчанию |
| Проверка полноты | Обязательные поля существуют | Заполнить значения по умолчанию |
| Проверка согласованности | Межполевые отношения корректны | Пометить для проверки |

## Стратегия Слияния Конфигураций

### Слияние на Уровне Полей

```mermaid
flowchart TB
    subgraph Input
        A[Официальная Конфигурация]
        B[Конфигурация Сообщества]
        C[Пользовательская Конфигурация]
    end

    subgraph Merge Process
        D[По приоритету полей]
        E[Сохранить не-null значения]
        F[Проверить результат]
    end

    A --> D
    B --> D
    C --> D
    D --> E
    E --> F
    F --> G[Эффективная Конфигурация]
```

### Пример Слияния

| Поле | Официальное Значение | Значение Сообщества | Пользовательское Значение | Финальное Значение |
| --- | --- | --- | --- | --- |
| context_window | 128000 | - | 64000 | 64000 |
| max_concurrent | 100 | 50 | - | 50 |
| pricing_model | PayAsYouGo | - | - | PayAsYouGo |

## Интерфейс Пользовательской Конфигурации

### Структура Файла Конфигурации

```mermaid
graph TB
    subgraph User Config File
        A[Отображаемое имя провайдера]
        B[Настройки типа использования]
        C[Ограничения квоты]
        D[Контроль параллелизма]
        E[Управление контекстом]
        F[Переопределения моделей]
    end

    A --> A1[Пользовательское отображаемое имя]
    B --> B1[metered/quota/unlimited]
    C --> C1[Лимит данных/Период восстановления]
    D --> D1[Макс. одновременных]
    E --> E1[Теоретический лимит/Практический лимит]
    F --> F1[Пользовательский список моделей]
```

## Механизм Запланированного Обновления

```mermaid
sequenceDiagram
    participant Timer as Таймер
    participant Queue as Очередь Задач
    participant Agent as Пул Агентов
    participant DB as База Данных

    Timer->>Queue: Добавить задачу обновления
    Queue->>Agent: Назначить задачу

    loop Каждый Провайдер
        Agent->>Agent: Получить последнюю конфигурацию
        Agent->>DB: Сравнить изменения
        alt Есть изменения
            DB->>DB: Обновить конфигурацию
            DB->>DB: Записать изменения
        else Нет изменений
            DB->>DB: Обновить время проверки
        end
    end

    Agent-->>Queue: Задача завершена
```

## Обработка Ошибок

### Обработка Сбоя Сбора

```mermaid
flowchart TB
    A[Сбор завершился сбоем] --> B{Тип сбоя}
    B -->|Сетевая ошибка| C[Механизм повторных попыток]
    B -->|Ошибка разбора| D[Записать и пропустить]
    B -->|Ошибка проверки| E[Пометить для проверки]

    C --> F{Количество попыток}
    F -->|Не превышено| G[Отложенная повторная попытка]
    F -->|Превышено| H[Использовать кэшированные данные]

    G --> A
    D --> I[Продолжить следующее]
    E --> J[Очередь ручной проверки]
```

## Проект Расширяемости

### Добавление Нового Провайдера

```mermaid
flowchart LR
    A[Определить Агента] --> B[Реализовать интерфейс сбора]
    B --> C[Настроить правила разбора]
    C --> D[Зарегистрировать в планировщике]
    D --> E[Начать сбор]
```

### Точки Расширения

| Тип Расширения | Описание | Реализация |
| --- | --- | --- |
| Новый Провайдер | Добавить новый источник конфигурации | Реализовать интерфейс Агента Провайдера |
| Новое поле | Расширить структуру конфигурации | Обновить модель данных и правила проверки |
| Новое правило проверки | Добавить логику проверки | Добавить реализацию валидатора |

## Реализация Агента Уровня 3

### Агент ProviderScratch

`ProviderScratch` — первый официальный Агент Уровня 3, служащий примером реализации средств скрапинга.

```mermaid
flowchart TB
    subgraph ProviderScratch Agent
        A[Вход Агента] --> B{Режим Выполнения}
        B -->|Режим TUI| C[Интерактивный Интерфейс]
        B -->|Режим CI| D[Автоматизированное Выполнение]

        C --> E[Выбрать Провайдера]
        D --> F[Прочитать переменные окружения]

        E --> G[Вызвать Навык]
        F --> G

        G --> H[Скрапинг документации]
        H --> I[Разбор данных]
        I --> J[Генерация TOML]

        J --> K{Подтвердить коммит?}
        K -->|Да| L[Записать в рабочее пространство]
        K -->|Нет| M[Отбросить изменения]

        L --> N[Запросить коммит пользователя]
    end
```

### Архитектура Навыков

Каждый Провайдер соответствует независимому Навыку:

```mermaid
graph LR
    subgraph Skills
        A[openai]
        B[anthropic]
        C[glm]
        D[deepseek]
        E[qwen]
        F[gemini]
    end

    subgraph Shared Components
        G[Скрапер Документации]
        H[Парсер Данных]
        I[Генератор TOML]
    end

    A --> G
    B --> G
    C --> G
    D --> G
    E --> G
    F --> G

    G --> H
    H --> I
```

### Структура Директории

```mermaid
flowchart LR
    Root[".amphoreus/provider_scratch/"]
    AT["agent.toml"]
    OV["overview/"]
    SK["skills/"]
    Root --> AT
    Root --> OV
    Root --> SK
    OV --> ZH["zhs.md"]
    SK --> OA["openai/"]
    SK --> AN["anthropic/"]
    SK --> GL["glm/"]
    SK --> DS["deepseek/"]
    SK --> QW["qwen/"]
    SK --> GE["gemini/"]
    OA --> OAP["prompt.md"]
    AN --> ANP["prompt.md"]
    GL --> GLP["prompt.md"]
    DS --> DSP["prompt.md"]
    QW --> QWP["prompt.md"]
    GE --> GEP["prompt.md"]
```

### Автоматизация CI

```mermaid
flowchart LR
    A[Запланированный триггер] --> B[Извлечь код]
    B --> C[Запустить ProviderScratch]
    C --> D{Обнаружить изменения}
    D -->|Есть изменения| E[Создать ветку]
    E --> F[Зафиксировать изменения]
    F --> G[Создать PR]
    G --> H[Ожидать проверки]
    D -->|Нет изменений| I[Завершено]
```

### Переменные Окружения

| Имя Переменной | Описание |
| --- | --- |
| `AMPHOREUS_PROVIDER_SCRATCH_PROVIDERS` | Список провайдеров для скрапинга |
| `AMPHOREUS_PROVIDER_SCRATCH_OUTPUT_DIR` | Путь к выходной директории |
| `AMPHOREUS_PROVIDER_SCRATCH_GIT_BRANCH` | Целевая Git ветка |
| `AMPHOREUS_PROVIDER_SCRATCH_DRY_RUN` | Только пробный запуск |

## Будущие Планы

| Функция | Описание | Приоритет |
| --- | --- | --- |
| Контроль версий конфигурации | Отслеживание истории изменений конфигурации | Высокий |
| Уведомление об изменениях | Уведомление пользователей об обновлениях конфигурации | Средний |
| Откат конфигурации | Поддержка отката к историческим версиям | Средний |
| Умные рекомендации | Рекомендация конфигураций на основе шаблонов использования | Низкий |
| GitHub巡回 Агент | Авто-создание PR для обновления конфигураций | Высокий |
