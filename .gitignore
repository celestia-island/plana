/target
Cargo.lock
*.swp
*.swo
*~
.DS_Store
__pycache__/
*.pyc

dist/

# celestia-devtools: staged-on-demand shared justfile recipes (just fetch)
/.just/
/celestia-devtools.just
.cargo/config.toml
node_modules/
# Bare form too: worktree node_modules may be a symlink (AGENTS.md §9.6),
# which the directory-only pattern above does not match.
node_modules
