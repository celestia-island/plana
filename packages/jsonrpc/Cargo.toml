[package]
name = "plana-jsonrpc"
version = "0.2.0"
edition.workspace = true
rust-version.workspace = true
license-file.workspace = true
authors.workspace = true
description = "Re-export shim for the JSON-RPC 2.0 wire layer, now absorbed into the plana protocol foundation (plana::jsonrpc). Kept so existing pins keep compiling; depend on plana directly in new code."
publish = false

[lints]
workspace = true

[dependencies]
plana = { version = "0.2.0", path = "../plana" }
