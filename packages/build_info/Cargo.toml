[package]
name = "plana_build_info"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false
license-file.workspace = true

[lints]
workspace = true

[dependencies]
# Digest behind the unified build token (see the crate docs): the same
# SHA-256 → base32 → last-6 shape malkuth prints for supervised binaries.
sha2 = { workspace = true }
