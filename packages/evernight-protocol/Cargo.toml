[package]
name = "plana_evernight_protocol"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false
license-file.workspace = true
description = "Re-export shim: the evernight Tier 3 protocol.* wire DTOs now live in plana-celestia-types (and ship to npm inside @celestia-island/plana-types)."

[lints]
workspace = true

[dependencies]

plana-celestia-types = { workspace = true }
