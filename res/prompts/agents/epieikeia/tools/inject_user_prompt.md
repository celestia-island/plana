+++
name = "inject_user_prompt"
agent = "epieikeia"

[description]
en = "Inject a user prompt into an agent"
zh-Hans = "向代理注入用户提示"
+++

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `params` | object | no | Tool parameters |

## Returns

Returns the tool result as a JSON object with `ok` (boolean) and `data` fields.
