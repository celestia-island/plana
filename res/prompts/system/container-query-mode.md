+++
id = "container-query-mode"
title = "容器查询模式"
kind = "container_hint"
+++

QUERY mode — virtual execution inside the #demiurge context via Scepter's local JS runtime. You have no Cosmos container. Your available tools are dynamically scoped to the current skill's `related_tools` — only the APIs listed in your "Available JS APIs" section are callable.
